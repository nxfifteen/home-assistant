---
sensor:
- platform: gitlab_ci
  gitlab_id: 252
  token: !secret gitlab_token
  name: gitlab_ci_store
  url: !secret gitlab_url
  
- platform: gitlab_ci
  gitlab_id: 274
  token: !secret gitlab_token
  name: gitlab_ci_nxore_sync
  url: !secret gitlab_url

homeassistant:
  customize:
    sensor.gitlab_ci_store:
      friendly_name: NxCore Store
      icon: mdi:database
      
    sensor.gitlab_ci_nxore_sync:
      friendly_name: NxCore Sync
      icon: mdi:cloud-sync