---
alias: new_device_tracker
initial_state: true

trigger:
  - platform: event
    event_type: device_tracker_new_device
    
action:
  - service: notify.synchat_hab
    data_template:
      message: "New device joined the network - {{trigger.event.data.entity_id}}. #System #Network #Security"