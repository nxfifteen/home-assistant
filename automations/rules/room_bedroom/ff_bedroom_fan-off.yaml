---
# ## Fan Before Bed
alias: Turn off study fan after 20 minutes
trigger:
  - platform: state
    entity_id: switch.ff_bedroom_fan_switch
    to: 'on'
    for:
      minutes: 20

action:
    - service: switch.turn_off
      data:
        entity_id: switch.ff_bedroom_fan_switch