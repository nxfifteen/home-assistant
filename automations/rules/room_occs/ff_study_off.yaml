---
alias: The Study is Occupied
trigger:
  - platform: state
    entity_id: binary_sensor.room_occupancy_study
    to: 'off'
    
condition:
  condition: and
  conditions:
    - condition: state
      entity_id: binary_sensor.room_occupancy_study
      state: 'off'

action:
    - service: automation.trigger
      data:
        entity_id: automation.lighting_off_ff_study
