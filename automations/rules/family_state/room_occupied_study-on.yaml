---
alias: Room Occupied (Study)

trigger:
  - entity_id: binary_sensor.room_occupancy_study
    platform: state
    from:  'off'
    to:  'on'

action:
  - service: script.notify_join
    data_template:
      who: "stuart"
      title: 'Study Occupied'
      notification_id: "hass-room-study"
      value1: 'The Study is now occupied'